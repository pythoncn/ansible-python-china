---

- include: install.yml


- include: config.yml
  tags: [deploy]


- name: Restart supervisor task
  supervisorctl: name={{ zerqu_app_name }} state=restarted
  notify:
    - newrelic deployment
  tags: [deploy]
